<launch>
  <arg name="launch_driver" default="true"/>
  <arg name="imu_serial_port" default="/dev/imu_usb" description="Serial port for IMU (use udev symlink or /dev/ttyUSB*)"/>
  <arg name="imu_baud_rate" default="9600" description="Baud rate for IMU serial communication (9600 or 921600)"/>
  <arg name="imu_frame_id" default="imu_link" description="Frame ID for IMU messages"/>
  <arg name="vehicle_id" default="default"/>

  <group>
    <push-ros-namespace namespace="imu"/>

    <!-- WitMotion/Yahboom 10-Axis IMU Driver -->
    <!-- This driver works with Yahboom IMU (WitMotion based) -->
    <!-- Default port: /dev/imu_usb (set up udev rules) or use /dev/ttyUSB* -->
    <!-- Supports baud rates: 4800-921600 bps (default 9600) -->
    <group if="$(var launch_driver)">
      <push-ros-namespace namespace="wit"/>
      <node pkg="wit_ros2_imu" name="wit_imu_node" exec="wit_ros2_imu" output="screen" respawn="true" respawn_delay="1.0">
        <!-- Load parameters from config file -->
        <param from="$(find-pkg-share reva_sensor_kit_launch)/config/wit_imu_driver.param.yaml"/>
        
        <!-- Override with launch arguments if provided -->
        <param name="port" value="$(var imu_serial_port)"/>
        <param name="baud_rate" value="$(var imu_baud_rate)"/>
        <param name="frame_id" value="$(var imu_frame_id)"/>
        
        <!-- Topic remapping -->
        <remap from="imu/data_raw" to="imu_raw"/>
      </node>
    </group>

    <!-- IMU Corrector Configuration -->
    <arg name="imu_raw_name" default="wit/imu_raw"/>
    <arg name="imu_corrector_param_file" default="$(find-pkg-share individual_params)/config/$(var vehicle_id)/reva_sensor_kit/imu_corrector.param.yaml"/>
    
    <!-- IMU Corrector - Applies calibration and bias correction -->
    <include file="$(find-pkg-share autoware_imu_corrector)/launch/imu_corrector.launch.xml">
      <arg name="input_topic" value="$(var imu_raw_name)"/>
      <arg name="output_topic" value="imu_data"/>
      <arg name="param_file" value="$(var imu_corrector_param_file)"/>
    </include>

    <!-- Gyro Bias Estimator - Estimates and removes gyro bias during vehicle motion -->
    <include file="$(find-pkg-share autoware_imu_corrector)/launch/gyro_bias_estimator.launch.xml">
      <arg name="input_imu_raw" value="$(var imu_raw_name)"/>
      <arg name="input_twist" value="/sensing/vehicle_velocity_converter/twist_with_covariance"/>
      <arg name="imu_corrector_param_file" value="$(var imu_corrector_param_file)"/>
    </include>
  </group>
</launch>
